// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/usegranthq/forge/ent/predicate"
	"github.com/usegranthq/forge/ent/project"
	"github.com/usegranthq/forge/ent/projectdomain"
)

// ProjectDomainQuery is the builder for querying ProjectDomain entities.
type ProjectDomainQuery struct {
	config
	ctx         *QueryContext
	order       []projectdomain.OrderOption
	inters      []Interceptor
	predicates  []predicate.ProjectDomain
	withProject *ProjectQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectDomainQuery builder.
func (pdq *ProjectDomainQuery) Where(ps ...predicate.ProjectDomain) *ProjectDomainQuery {
	pdq.predicates = append(pdq.predicates, ps...)
	return pdq
}

// Limit the number of records to be returned by this query.
func (pdq *ProjectDomainQuery) Limit(limit int) *ProjectDomainQuery {
	pdq.ctx.Limit = &limit
	return pdq
}

// Offset to start from.
func (pdq *ProjectDomainQuery) Offset(offset int) *ProjectDomainQuery {
	pdq.ctx.Offset = &offset
	return pdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pdq *ProjectDomainQuery) Unique(unique bool) *ProjectDomainQuery {
	pdq.ctx.Unique = &unique
	return pdq
}

// Order specifies how the records should be ordered.
func (pdq *ProjectDomainQuery) Order(o ...projectdomain.OrderOption) *ProjectDomainQuery {
	pdq.order = append(pdq.order, o...)
	return pdq
}

// QueryProject chains the current query on the "project" edge.
func (pdq *ProjectDomainQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: pdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectdomain.Table, projectdomain.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectdomain.ProjectTable, projectdomain.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectDomain entity from the query.
// Returns a *NotFoundError when no ProjectDomain was found.
func (pdq *ProjectDomainQuery) First(ctx context.Context) (*ProjectDomain, error) {
	nodes, err := pdq.Limit(1).All(setContextOp(ctx, pdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectdomain.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pdq *ProjectDomainQuery) FirstX(ctx context.Context) *ProjectDomain {
	node, err := pdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectDomain ID from the query.
// Returns a *NotFoundError when no ProjectDomain ID was found.
func (pdq *ProjectDomainQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdq.Limit(1).IDs(setContextOp(ctx, pdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectdomain.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pdq *ProjectDomainQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectDomain entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectDomain entity is found.
// Returns a *NotFoundError when no ProjectDomain entities are found.
func (pdq *ProjectDomainQuery) Only(ctx context.Context) (*ProjectDomain, error) {
	nodes, err := pdq.Limit(2).All(setContextOp(ctx, pdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectdomain.Label}
	default:
		return nil, &NotSingularError{projectdomain.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pdq *ProjectDomainQuery) OnlyX(ctx context.Context) *ProjectDomain {
	node, err := pdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectDomain ID in the query.
// Returns a *NotSingularError when more than one ProjectDomain ID is found.
// Returns a *NotFoundError when no entities are found.
func (pdq *ProjectDomainQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdq.Limit(2).IDs(setContextOp(ctx, pdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectdomain.Label}
	default:
		err = &NotSingularError{projectdomain.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pdq *ProjectDomainQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectDomains.
func (pdq *ProjectDomainQuery) All(ctx context.Context) ([]*ProjectDomain, error) {
	ctx = setContextOp(ctx, pdq.ctx, ent.OpQueryAll)
	if err := pdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectDomain, *ProjectDomainQuery]()
	return withInterceptors[[]*ProjectDomain](ctx, pdq, qr, pdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pdq *ProjectDomainQuery) AllX(ctx context.Context) []*ProjectDomain {
	nodes, err := pdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectDomain IDs.
func (pdq *ProjectDomainQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pdq.ctx.Unique == nil && pdq.path != nil {
		pdq.Unique(true)
	}
	ctx = setContextOp(ctx, pdq.ctx, ent.OpQueryIDs)
	if err = pdq.Select(projectdomain.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pdq *ProjectDomainQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pdq *ProjectDomainQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pdq.ctx, ent.OpQueryCount)
	if err := pdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pdq, querierCount[*ProjectDomainQuery](), pdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pdq *ProjectDomainQuery) CountX(ctx context.Context) int {
	count, err := pdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pdq *ProjectDomainQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pdq.ctx, ent.OpQueryExist)
	switch _, err := pdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pdq *ProjectDomainQuery) ExistX(ctx context.Context) bool {
	exist, err := pdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectDomainQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pdq *ProjectDomainQuery) Clone() *ProjectDomainQuery {
	if pdq == nil {
		return nil
	}
	return &ProjectDomainQuery{
		config:      pdq.config,
		ctx:         pdq.ctx.Clone(),
		order:       append([]projectdomain.OrderOption{}, pdq.order...),
		inters:      append([]Interceptor{}, pdq.inters...),
		predicates:  append([]predicate.ProjectDomain{}, pdq.predicates...),
		withProject: pdq.withProject.Clone(),
		// clone intermediate query.
		sql:  pdq.sql.Clone(),
		path: pdq.path,
	}
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (pdq *ProjectDomainQuery) WithProject(opts ...func(*ProjectQuery)) *ProjectDomainQuery {
	query := (&ProjectClient{config: pdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pdq.withProject = query
	return pdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Domain string `json:"domain,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectDomain.Query().
//		GroupBy(projectdomain.FieldDomain).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pdq *ProjectDomainQuery) GroupBy(field string, fields ...string) *ProjectDomainGroupBy {
	pdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectDomainGroupBy{build: pdq}
	grbuild.flds = &pdq.ctx.Fields
	grbuild.label = projectdomain.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Domain string `json:"domain,omitempty"`
//	}
//
//	client.ProjectDomain.Query().
//		Select(projectdomain.FieldDomain).
//		Scan(ctx, &v)
func (pdq *ProjectDomainQuery) Select(fields ...string) *ProjectDomainSelect {
	pdq.ctx.Fields = append(pdq.ctx.Fields, fields...)
	sbuild := &ProjectDomainSelect{ProjectDomainQuery: pdq}
	sbuild.label = projectdomain.Label
	sbuild.flds, sbuild.scan = &pdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectDomainSelect configured with the given aggregations.
func (pdq *ProjectDomainQuery) Aggregate(fns ...AggregateFunc) *ProjectDomainSelect {
	return pdq.Select().Aggregate(fns...)
}

func (pdq *ProjectDomainQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pdq); err != nil {
				return err
			}
		}
	}
	for _, f := range pdq.ctx.Fields {
		if !projectdomain.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pdq.path != nil {
		prev, err := pdq.path(ctx)
		if err != nil {
			return err
		}
		pdq.sql = prev
	}
	return nil
}

func (pdq *ProjectDomainQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectDomain, error) {
	var (
		nodes       = []*ProjectDomain{}
		withFKs     = pdq.withFKs
		_spec       = pdq.querySpec()
		loadedTypes = [1]bool{
			pdq.withProject != nil,
		}
	)
	if pdq.withProject != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, projectdomain.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectDomain).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectDomain{config: pdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pdq.withProject; query != nil {
		if err := pdq.loadProject(ctx, query, nodes, nil,
			func(n *ProjectDomain, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pdq *ProjectDomainQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectDomain, init func(*ProjectDomain), assign func(*ProjectDomain, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectDomain)
	for i := range nodes {
		if nodes[i].project_domain == nil {
			continue
		}
		fk := *nodes[i].project_domain
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_domain" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pdq *ProjectDomainQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pdq.querySpec()
	_spec.Node.Columns = pdq.ctx.Fields
	if len(pdq.ctx.Fields) > 0 {
		_spec.Unique = pdq.ctx.Unique != nil && *pdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pdq.driver, _spec)
}

func (pdq *ProjectDomainQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectdomain.Table, projectdomain.Columns, sqlgraph.NewFieldSpec(projectdomain.FieldID, field.TypeUUID))
	_spec.From = pdq.sql
	if unique := pdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pdq.path != nil {
		_spec.Unique = true
	}
	if fields := pdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectdomain.FieldID)
		for i := range fields {
			if fields[i] != projectdomain.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pdq *ProjectDomainQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pdq.driver.Dialect())
	t1 := builder.Table(projectdomain.Table)
	columns := pdq.ctx.Fields
	if len(columns) == 0 {
		columns = projectdomain.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pdq.sql != nil {
		selector = pdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pdq.ctx.Unique != nil && *pdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pdq.predicates {
		p(selector)
	}
	for _, p := range pdq.order {
		p(selector)
	}
	if offset := pdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectDomainGroupBy is the group-by builder for ProjectDomain entities.
type ProjectDomainGroupBy struct {
	selector
	build *ProjectDomainQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pdgb *ProjectDomainGroupBy) Aggregate(fns ...AggregateFunc) *ProjectDomainGroupBy {
	pdgb.fns = append(pdgb.fns, fns...)
	return pdgb
}

// Scan applies the selector query and scans the result into the given value.
func (pdgb *ProjectDomainGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdgb.build.ctx, ent.OpQueryGroupBy)
	if err := pdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectDomainQuery, *ProjectDomainGroupBy](ctx, pdgb.build, pdgb, pdgb.build.inters, v)
}

func (pdgb *ProjectDomainGroupBy) sqlScan(ctx context.Context, root *ProjectDomainQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pdgb.fns))
	for _, fn := range pdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pdgb.flds)+len(pdgb.fns))
		for _, f := range *pdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectDomainSelect is the builder for selecting fields of ProjectDomain entities.
type ProjectDomainSelect struct {
	*ProjectDomainQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pds *ProjectDomainSelect) Aggregate(fns ...AggregateFunc) *ProjectDomainSelect {
	pds.fns = append(pds.fns, fns...)
	return pds
}

// Scan applies the selector query and scans the result into the given value.
func (pds *ProjectDomainSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pds.ctx, ent.OpQuerySelect)
	if err := pds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectDomainQuery, *ProjectDomainSelect](ctx, pds.ProjectDomainQuery, pds, pds.inters, v)
}

func (pds *ProjectDomainSelect) sqlScan(ctx context.Context, root *ProjectDomainQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pds.fns))
	for _, fn := range pds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
